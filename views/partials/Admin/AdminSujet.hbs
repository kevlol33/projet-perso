&nbsp;
<div class="container">
    <div class="row">
        <table class="table table-hover table-dark">
            <thead>
                <tr>
                    <th scope="col">Titre</th>
                    <th scope="col">Type</th>
                    <th scope="col">Description</th>
                    <th scope="col">Bouton</th>
                </tr>
            </thead>
            {{#each dbsujets }}
            <tr>
                <td>{{ title }}</td>
                <td>{{ type }}</td>
                <td>{{ description }}</td>
                <td> <a href="/Sujet/{{_id}}" class="btn btn-primary"> Voir L'article</a></td>
            </tr>
            {{/each }}
        </table>
    </div>
</div>