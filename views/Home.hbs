<div class="container">
    <div class="row">
        <h1 class="title is-1">Favorie du mois :</h1>
        <div class="col-md-12">
            <div class="card-deck">
                {{#each dbsujets}}
                <div class="card text-white bg-secondary mb-3">
                    <img src="{{imgSujets}}" class="card-img-top" alt="...">
                    <div class="card-body">
                        <h5 class="card-title"> {{title}} </h5>
                        <p class="card-text">{{ description }}</p>
                        <a href="/Sujet/{{_id}}"> Voir L'article</a></td>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>
    </div>


    <div class="container">
        <div class="row">
            <h1 class="title is-1">Favorie du mois :</h1>
            <div class="col-md-12">
                <div class="card-deck">
                    {{#each dbsujets}}
                    <div class="card text-white bg-secondary mb-3">
                        <img src="{{imgSujets}}" class="card-img-top" alt="...">
                        <div class="card-body">
                            <h5 class="card-title"> {{title}} </h5>
                            <p class="card-text">{{ description }}</p>
                            <a href="/Sujet/{{_id}}"> Voir L'article</a></td>
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>
        </div>